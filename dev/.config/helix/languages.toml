[[language]]
name = "go"
scope = "source.go"
injection-regex = "go"
file-types = ["go"]
roots = ["go.work", "go.mod"]
auto-format = true
comment-token = "//"
block-comment-tokens = { start = "/*", end = "*/" }
formatter = ["gofmt"]
language-servers = [ "gopls", "golangci-lint-lsp", "codebook" ]
# TODO: gopls needs utf-8 offsets?
indent = { tab-width = 2, unit = "\t" }

[language-server.golangci-lint-lsp]
command = "golangci-lint-langserver"

[language-server.golangci-lint-lsp.config]
command = ["golangci-lint", "run", "--output.json.path=stdout", "--show-stats=false", "--issues-exit-code=1"]

[language-server.gopls]
command = "gopls"

[language-server.gopls.config]
"local" = "goimports"
"staticcheck" = true
"verboseOutput" = true
completeUnimported = true
"semanticTokens" = true
usePlaceholders = true

[language-server.gopls.config.analyses]
"nilness" = true
unusedparams = true
unusedwrite = true
useany = true

[language-server.gopls.config.hints]
assignVariableTypes = true
compositeLiteralFields = true
constantValues = true
functionTypeParameters = true
parameterNames = true
rangeVariableTypes = true
"assignVariableType" = true
"compositeLiteralTypes" = true

[[language]]
name = "rust"
formatter = { command = "rustfmt" }

[language-server.codebook]
command = "codebook-lsp"
args = ["serve"]


[[language]]
name = "markdown"
language-servers = ["codebook"]


# --- Ada ---------------------------------------------------------------------
# Formatting is done by ALS over LSP (it wraps GNATformat), so no external
# formatter command is needed. Debugging uses GDB's own DAP interpreter,
# which is the only adapter with real Ada type/array support.

[[language]]
name = "ada"
auto-format = true
indent = { tab-width = 3, unit = "   " }
language-servers = ["ada-language-server"]

[language.debugger]
name = "gdb"
transport = "stdio"
command = "gdb"
args = ["--interpreter=dap"]

[[language.debugger.templates]]
name = "binary"
request = "launch"
completion = [{ name = "binary", completion = "filename" }]
args = { program = "{0}" }

[[language.debugger.templates]]
name = "binary (stop at main)"
request = "launch"
completion = [{ name = "binary", completion = "filename" }]
args = { program = "{0}", stopAtBeginningOfMainSubprogram = true }

[[language.debugger.templates]]
name = "attach"
request = "attach"
completion = [{ name = "pid" }]
args = { pid = "{0}" }

[language-server.ada-language-server]
command = "ada_language_server"
# command = "alr"
# args = ["exec", "--", "ada_language_server"]

# Same server handles .gpr files.
[[language]]
name = "gpr"
language-servers = ["ada-gpr-language-server"]

[language-server.ada-gpr-language-server]
command = "ada_language_server"
args = ["--language-gpr"]
# command = "alr"
# args = ["exec", "--", "ada_language_server", "--language-gpr"]